/**
 * activity feature 的 renderer 端類型 —— 前身 features/project-flow/types。
 * 主類型 re-export 自跨進程共用 `@shared/types/activity.types`。
 */

export type {
    TodayActivityCategory,
    TodayActivitySummary,
} from '@shared/types/activity.types'
