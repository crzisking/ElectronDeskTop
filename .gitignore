/dist
/node_modules
/out
/.claude
/.idea
/logs
app.db

# 環境變量（帶 .local 後綴的包含本機密碼/Token，不提交）
# .env.development 和 .env.production 本身可以提交（無敏感信息）
.env.*.local
.env.local

# 由 unplugin-auto-import / unplugin-vue-components 自動生成，每次構建會重寫
src/types/auto-imports.d.ts
src/types/components.d.ts

