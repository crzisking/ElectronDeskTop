# 功能設計文檔

## 1. 登錄功能（預留）

### 1.1 設計說明

登錄功能當前**不實現業務邏輯**，但需完整預留以下內容，確保後續開發無需大量重構。

### 1.2 預留內容清單

| 預留項 | 位置 | 說明 |
|--------|------|------|
| 路由 `/login` | `src/router/index.ts` | 路由已定義，路由守衛邏輯已注釋 |
| `LoginView.vue` | `src/views/Login/` | 頁面已創建，顯示"開發中"占位 |
| `auth.store.ts` | `src/stores/` | State/Actions 已定義，接口已聲明 |
| `auth.api.ts` | `src/api/modules/` | 接口函數已聲明，實現為 TODO |
| `auth.handlers.ts` | `electron/main/ipc-handlers/` | IPC 處理器已注冊，keytar 調用已實現 |
| Token 攔截器 | `src/api/interceptors/auth.interceptor.ts` | 攔截器已就位，token 從 store 讀取 |

### 1.3 auth.store.ts 設計

```typescript
// src/stores/auth.store.ts
interface AuthState {
  isAuthenticated: boolean
  user: UserProfile | null
  accessToken: string | null   // 僅內存，來源是 OS 鑰匙串
}

interface UserProfile {
  id: string
  name: string
  email: string
  avatar?: string
  department?: string
}

// Actions
// login(credentials: LoginCredentials): Promise<void>
//   → POST /auth/login
//   → 存儲 token 到 OS 鑰匙串（via IPC）
//   → 更新 isAuthenticated = true
//
// logout(): Promise<void>
//   → 清除 OS 鑰匙串中的 token（via IPC）
//   → 重置 state
//   → 跳轉 /login
//
// restoreSession(): Promise<void>
//   → 讀取 OS 鑰匙串 token（via IPC）
//   → 若有效，設置 accessToken 並 isAuthenticated = true
//   → 可選：調用 /auth/verify 驗證 token 是否過期
```

---

## 2. 統一平台頁面

### 2.1 功能概述

為用戶提供公司各內部系統的統一入口，支持：
- 系統卡片展示（圖標、名稱、描述）
- 點擊後在 iframe 內嵌打開或在外部瀏覽器打開
- SSO 直通：將 Auth Token 作為查詢參數注入 URL，實現單點登錄

### 2.2 頁面佈局

```
┌──────────────────────────────────────────────────────┐
│  統一平台                                    [搜索框] │
├──────────────────────────────────────────────────────┤
│                                                      │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐│
│  │  [圖標] │  │  [圖標] │  │  [圖標] │  │  [圖標] ││
│  │         │  │         │  │         │  │         ││
│  │ ERP系統 │  │ OA系統  │  │ 工單系統 │  │ HR系統  ││
│  │ 企業資源│  │ 辦公自動│  │ 問題追蹤│  │ 人力資源││
│  │ [打開]  │  │ [打開]  │  │ [打開]  │  │ [打開]  ││
│  └─────────┘  └─────────┘  └─────────┘  └─────────┘│
│                                                      │
│  ┌─────────┐  ┌─────────┐                           │
│  │  [圖標] │  │  [圖標] │                           │
│  │         │  │         │                           │
│  │ 知識庫  │  │ 監控平台 │                           │
│  │ 文檔中心│  │ 系統監控│                           │
│  │ [打開]  │  │ [打開]  │                           │
│  └─────────┘  └─────────┘                           │
└──────────────────────────────────────────────────────┘
```

當用戶選擇 iframe 模式打開某系統時，頁面切換為：

```
┌──────────────────────────────────────────────────────┐
│  ← 返回列表    ERP 系統               [在瀏覽器打開] │
├──────────────────────────────────────────────────────┤
│                                                      │
│  ┌────────────────────────────────────────────────┐  │
│  │                                                │  │
│  │                iframe 內容                     │  │
│  │         （公司 ERP 系統界面）                  │  │
│  │                                                │  │
│  │                                                │  │
│  └────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────┘
```

### 2.3 SSO 直通邏輯

```
用戶點擊系統卡片
       │
       ▼
讀取 SystemLink.ssoEnabled
       │
    ┌──┴──┐
    │true │false
    ▼     ▼
從 authStore 讀取   直接使用原始 URL
accessToken
    │
    ▼
URL = `${system.url}?${system.ssoTokenParam}=${token}`
    │
    ▼
根據 openMode：
  'iframe'           → 在 IframeContainer 中加載
  'external-browser' → shell.openExternal(url)
```

### 2.4 核心組件

**`SystemCard.vue`**

```typescript
// Props
interface Props {
  system: SystemLink  // 來自 config.unifiedPlatform.systems
}

// Emits
// 'open': 用戶點擊打開按鈕
```

**`IframeContainer.vue`**

```typescript
// Props
interface Props {
  src: string
  title: string
  allowFullscreen?: boolean
}

// 安全考量：
// sandbox="allow-scripts allow-same-origin allow-forms allow-popups"
// 不允許 allow-top-navigation（防止 iframe 劫持主窗口導航）
```

---

## 3. AI 快捷功能

### 3.1 功能概述

提供三個 AI 工具，調用公司 AI 後端 API：

| 工具 | 功能 | API |
|------|------|-----|
| 文本處理 | 潤色/翻譯/格式化文本 | `POST /ai/text-process` |
| 摘要生成 | 長文本壓縮為摘要 | `POST /ai/summarize` |
| 智能問答 | 對話式問答 | `POST /ai/qa` (SSE 流式) |

### 3.2 頁面佈局

```
┌──────────────────────────────────────────────────────┐
│  AI 快捷功能                                         │
├──────────────────────────────────────────────────────┤
│  [文本處理]  [摘要生成]  [智能問答]                  │
├──────────────────────────────────────────────────────┤
│                                                      │
│  ── 文本處理 ────────────────────────────────────    │
│                                                      │
│  操作類型: [潤色文本 ▼]  [翻譯:中→英 ▼]             │
│                                                      │
│  輸入：                                              │
│  ┌────────────────────────────────────────────────┐  │
│  │                                                │  │
│  │  在這裡輸入需要處理的文本...                    │  │
│  │                                                │  │
│  └────────────────────────────────────────────────┘  │
│  字數：0 / 4000                    [清空] [處理 →]   │
│                                                      │
│  結果：                                              │
│  ┌────────────────────────────────────────────────┐  │
│  │                                                │  │
│  │  （AI 處理後的文本顯示在這裡）                  │  │
│  │                                                │  │
│  └────────────────────────────────────────────────┘  │
│                                            [複製結果] │
└──────────────────────────────────────────────────────┘
```

### 3.3 流式輸出支持（SSE）

智能問答支持 Server-Sent Events 流式輸出，實現打字機效果：

```typescript
// src/composables/useAiStream.ts
async function sendQuestion(question: string) {
  const response = await fetch(`${config.aiBaseUrl}/qa`, {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${authStore.accessToken}`,
      'Accept': 'text/event-stream',
    },
    body: JSON.stringify({ question })
  })

  const reader = response.body!.getReader()
  const decoder = new TextDecoder()

  while (true) {
    const { done, value } = await reader.read()
    if (done) break
    const chunk = decoder.decode(value)
    // 解析 SSE data: 字段，追加到 answerText
    parseSSEChunk(chunk).forEach(text => {
      answerText.value += text
    })
  }
}
```

### 3.4 API 接口定義

```typescript
// src/api/modules/ai.api.ts
interface TextProcessRequest {
  text: string
  operation: 'polish' | 'translate' | 'format'
  language?: 'zh' | 'en' | 'ja'
}

interface SummarizeRequest {
  text: string
  maxLength?: number
}

interface QaRequest {
  question: string
  context?: string
  history?: Array<{ role: 'user' | 'assistant'; content: string }>
}

interface AiResponse {
  result: string
  usage: { inputTokens: number; outputTokens: number }
}
```

---

## 4. 快速聯繫

### 4.1 功能概述

幫助用戶快速找到某個問題/業務的負責人，並通過系統直接發送郵件，無需手動打開郵件客戶端。

### 4.2 功能流程

```
用戶輸入問題關鍵詞
（例如："ERP 系統故障" / "報銷審批" / "網絡問題"）
         │
         ▼ 調用 GET /contacts/search?q=xxx
         │
    ┌────┴────┐
    │搜索結果  │
    └────┬────┘
         │
         ▼
顯示聯繫人列表：
  ┌────────────────────────────────────┐
  │ [頭像] 張三  IT 運維組              │
  │        負責：網絡/伺服器/系統故障   │
  │        📧 zhangsan@company.com     │
  │                     [聯繫他]       │
  ├────────────────────────────────────┤
  │ [頭像] 李四  財務部                 │
  │        負責：報銷/費用審批          │
  │        📧 lisi@company.com        │
  │                     [聯繫他]       │
  └────────────────────────────────────┘
         │
   用戶點擊 [聯繫他]
         │
         ▼
  彈出郵件編輯器：
  ┌────────────────────────────────────┐
  │ 收件人: 張三 <zhangsan@company.com>│
  │ 主 題: [自動預填：關於 "ERP 系統故障"] │
  │ 正 文:                             │
  │ ┌────────────────────────────────┐ │
  │ │                                │ │
  │ │  用戶輸入郵件內容...            │ │
  │ │                                │ │
  │ └────────────────────────────────┘ │
  │                 [取消]  [發送郵件] │
  └────────────────────────────────────┘
         │
   用戶點擊 [發送郵件]
         │
         ▼ 調用 POST /email/send
         │
    ┌────┴────┐
    │  成功   │  失敗
    │顯示成功 │  顯示錯誤
    │  提示   │  提示
    └─────────┘
```

### 4.3 組件設計

**`ContactSearch.vue`**

```typescript
// 防抖搜索，輸入停止 300ms 後觸發
const keyword = ref('')
const results = ref<Contact[]>([])
const isLoading = ref(false)

watch(keyword, debounce(async (val) => {
  if (!val.trim()) { results.value = []; return }
  isLoading.value = true
  results.value = await contactApi.search(val)
  isLoading.value = false
}, 300))
```

**`EmailComposer.vue`**

```typescript
// Props
interface Props {
  to: Contact
  defaultSubject?: string  // 預填主題（來自搜索關鍵詞）
}

// 表單校驗
const rules = {
  subject: [{ required: true, message: '請填寫主題' }],
  body:    [{ required: true, message: '請填寫郵件內容' }]
}
```

### 4.4 API 接口定義

---

## 5. 系統托盤

### 5.1 托盤菜單設計

托盤圖標始終存在（即使浮球隱藏），作為最後的訪問入口：

```
右鍵系統托盤圖標
       │
       ▼
┌─────────────────────┐
│  企業桌面客戶端      │
├─────────────────────┤
│  打開主窗口          │
├─────────────────────┤
│  統一平台            │
│  AI 快捷功能         │
│  快速聯繫            │
├─────────────────────┤
│  設置               │
├─────────────────────┤
│  退出               │
└─────────────────────┘
```

托盤菜單的"統一平台"、"AI 快捷功能"、"快速聯繫"三個菜單項，通過 `config.sidebar.items` 動態生成，與側邊欄菜單保持一致。

### 5.2 托盤實現要點

```typescript
// electron/main/tray-manager.ts

// 1. macOS 使用 Template 圖標（自動適應暗/亮色模式）
const iconPath = process.platform === 'darwin'
  ? 'resources/icons/tray-iconTemplate.png'
  : 'resources/icons/tray-icon.png'

// 2. 左鍵點擊：直接顯示主窗口（macOS 左鍵單擊托盤圖標）
tray.on('click', () => windowManager.showMainWindow())

// 3. 右鍵點擊：顯示托盤菜單
// （Windows 默認左鍵觸發，macOS 需右鍵，可監聽 right-click）
tray.on('right-click', () => tray.popUpContextMenu(contextMenu))
```
