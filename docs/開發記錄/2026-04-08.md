# 2026-04-08 開發記錄

## 新增功能：`electron-window` 第三種系統打開模式

### 背景

統一平台頁面原本只支持兩種系統打開方式：
- `iframe`：在主窗口內以 iframe 嵌入
- `external-browser`：用電腦默認瀏覽器打開

部分系統設置了 `X-Frame-Options` 禁止 iframe 嵌入，但用戶又不希望跳出應用到外部瀏覽器。因此新增第三種模式 `electron-window`：在 Electron 應用內新開一個獨立的 BrowserWindow 窗口加載網頁。

### 修改文件清單

| 文件 | 修改內容 |
|------|---------|
| `src/types/config.types.ts` | `SystemLink.openMode` 類型增加 `'electron-window'` 選項，並補充 JSDoc 說明 |
| `electron/shared/ipc-channels.ts` | 新增 `OPEN_CHILD_WINDOW: 'window:open-child'` IPC 頻道常量及完整文檔 |
| `electron/main/window-manager.ts` | 新增 `openChildWindow(url, title)` 方法，創建帶原生標題欄的子窗口 |
| `electron/main/ipc-handlers/index.ts` | 註冊 `OPEN_CHILD_WINDOW` 的 `ipcMain.handle` 處理器 |
| `electron/preload/index.ts` | 在 `window` 區塊新增 `openChild(url, title)` 方法，橋接到 IPC |
| `src/types/electron.d.ts` | 在 `window` 介面新增 `openChild` 類型聲明 |
| `src/views/UnifiedPlatform/UnifiedPlatformView.vue` | `handleOpenSystem()` 改為 switch-case，新增 `electron-window` 分支 |
| `src/views/UnifiedPlatform/SystemCard.vue` | openMode 標籤支持三種狀態：嵌入顯示(primary)、獨立窗口(warning)、外部瀏覽器(info) |
| `config/app-config.json` | 新增 `_help_openMode` 和 `_help_ssoEnabled` 欄位，說明 openMode 三種值的用途 |

### 完整 IPC 調用鏈（electron-window 模式）

```
用戶點擊系統卡片（openMode: 'electron-window'）
  → UnifiedPlatformView.vue: handleOpenSystem()
  → window.electronAPI.window.openChild(url, title)
  → preload/index.ts: ipcRenderer.invoke(IpcChannels.OPEN_CHILD_WINDOW, url, title)
  → ipc-handlers/index.ts: ipcMain.handle(OPEN_CHILD_WINDOW, ...)
  → window-manager.ts: openChildWindow(url, title)
  → 創建新 BrowserWindow，加載 URL
```

### 子窗口安全配置

```typescript
webPreferences: {
  contextIsolation: true,   // 渲染進程隔離
  nodeIntegration: false,   // 禁止 Node.js
  sandbox: false,           // 當前主窗口/浮球窗口實作為 false，確保 preload 能正常使用 Electron / Node 能力
}
```

子窗口使用原生標題欄（`frame: true`），不需要自定義標題欄和 preload 腳本。

---

## 為 app-config.json 全面添加注釋欄位

### 背景

JSON 格式不支持 `//` 或 `/* */` 注釋。為了讓開發人員在編輯配置時能快速理解每個欄位的用途和可選值，採用 `_comment` 前綴的欄位作為注釋。

### 注釋規範

- 文件開頭有一條全局說明：**所有以 `_comment` 開頭的欄位均為注釋，不參與程式邏輯**
- 每個配置區塊（`app`、`sidebar`、`floatingBall`、`unifiedPlatform`、`aiQuickFunctions`、`business`）都有 `_comment` 說明該區塊用途
- 每個關鍵欄位都有對應的 `_comment_欄位名` 注釋，說明可選值、單位、用途

### 修改文件

| 文件 | 修改內容 |
|------|---------|
| `config/app-config.json` | 所有區塊和關鍵欄位添加 `_comment` 注釋欄位 |

### 各區塊注釋清單

| 區塊 | 注釋欄位數量 | 涵蓋內容 |
|------|-------------|---------|
| 頂層 | 1 | 全局說明 `_comment` 欄位的性質 |
| `app` | 5 | language 可選值、theme 可選值、startMinimized、launchOnStartup |
| `sidebar` | 3 | defaultCollapsed、items 陣列結構說明 |
| `floatingBall` | 7 | size、opacity、defaultPosition、snapToEdge、quickMenu 結構、action.type 所有可選值 |
| `unifiedPlatform` | 6 | openMode 三種值詳細說明、ssoEnabled、ssoTokenParam、systems 結構 |
| `aiQuickFunctions` | 4 | apiBaseUrl、apiTimeout 單位、tools 結構 |
| `business` | 4 | pipelineApiEndpoint、ownerSearchApiEndpoint、maxSearchResults |

---

## 子窗口移除系統菜單欄

### 背景

使用 `electron-window` 模式打開子窗口時，Windows 系統會顯示默認菜單欄（File、Edit、View 等），對業務系統來說不需要這些操作。

### 修改文件

| 文件 | 修改內容 |
|------|---------|
| `electron/main/window-manager.ts` | `openChildWindow()` 添加 `autoHideMenuBar: true`，並調用 `setMenuBarVisibility(false)` + `removeMenu()` 徹底移除菜單欄 |

---

## 全面替換應用圖標為 ichia Logo

### 背景

將應用內所有圖標（除浮球外）統一替換為 ichia 公司 Logo，包括應用圖標、安裝包圖標、系統托盤圖標、以及 Vue 組件內的品牌圖標。

### 圖標生成

使用 `jimp` + `png-to-ico` 從原始 `ichia.png` 自動生成所有所需尺寸：

| 生成文件 | 尺寸 | 用途 |
|---------|------|------|
| `resources/icons/icon.ico` | 多尺寸(16~256) | Windows 應用圖標 + NSIS 安裝包圖標 |
| `resources/icons/icon.png` | 256x256 | Linux 應用圖標 |
| `resources/icons/tray-icon.png` | 32x32 | Windows/Linux 系統托盤 |
| `resources/icons/tray-iconTemplate.png` | 22x22 | macOS 系統托盤 |
| `src/assets/logo.png` | 128x128 | Vue 組件內使用 |

生成腳本：`scripts/generate-icons.mjs`

### 修改文件清單

| 文件 | 修改內容 |
|------|---------|
| `src/components/layout/SidebarNav.vue` | `<div class="logo-icon">企</div>` → `<img src="@/assets/logo.png">`，移除藍色背景樣式 |
| `src/components/layout/TitleBar.vue` | `<div class="app-icon">企</div>` → `<img src="@/assets/logo.png">`，圖標尺寸調大至 32x32 |
| `src/views/Login/LoginView.vue` | `<div class="brand-icon">企</div>` → `<img src="@/assets/logo.png">`，移除藍色背景樣式 |
| `electron/main/window-manager.ts` | 主窗口和子窗口 BrowserWindow 添加 `icon: appIconPath` 指向 `resources/icons/icon.ico`，解決 Windows 任務欄圖標問題 |

### package.json build 配置（無需修改）

圖標路徑已正確指向生成的文件：
- `win.icon` → `resources/icons/icon.ico`
- `mac.icon` → `resources/icons/icon.icns`
- `linux.icon` → `resources/icons/icon.png`

---

## 浮球圖標替換與樣式調整

### 背景

將浮球從原來的藍色漸變 +「企」字，改為白色圓球 + ichia Logo 圖片的樣式。

### 修改文件清單

| 文件 | 修改內容 |
|------|---------|
| `src/floating-ball/FloatingBall.vue` | `<span class="ball-text">企</span>` → `<img src="../assets/logo.png">`；背景改為白色圓球 + 陰影；移除拖動時的旋轉虛線圓環（`drag-ring`） |
| `src/floating-ball/App.vue` | 全局 `html, body` 尺寸從 `60x60` 更新為 `80x80`，與窗口尺寸一致 |
| `electron/main/window-manager.ts` | 浮球窗口尺寸從 `64x64` 調大為 `80x80` |

### 浮球最終樣式

- 白色圓形背景（`background: #fff`）
- 立體陰影（`box-shadow`）
- ichia Logo 居中顯示，佔球體 75%
- hover 時微放大（`scale(1.08)`）
- 拖動時微縮小（`scale(0.95)`）

---

## 清理多餘的根目錄 HTML 文件

### 背景

項目根目錄存在 `index.html` 和 `floating-ball.html` 兩個文件，與 `src/` 目錄下的同名文件重複。`electron.vite.config.ts` 配置的入口指向 `src/` 下的文件，根目錄的是早期開發殘留。

### 刪除文件

| 刪除 | 保留（實際入口） |
|------|----------------|
| `/index.html` | `src/index.html` |
| `/floating-ball.html` | `src/floating-ball.html` |

---

## 「快速聯繫」重構為「業務安排與尋找」

> **註：此功能模組（Business）在後續版本中已被移除，相關代碼不再保留於項目中。此處僅作為開發歷史記錄。**

### 背景

將原有的「快速聯繫」功能模組完全重構為「業務安排與尋找」，包含兩個子功能：
1. **維護業務流水線** — 使用 `@vue-flow/core` 可視化流程圖編輯器
2. **尋找業務負責人** — 根據業務關鍵詞搜索對應負責人

### 流程圖引擎選型歷程

最初選用 `@antv/x6`，但遇到以下問題導致切換到 `@vue-flow/core`：

1. **插件不兼容 v3**：`@antv/x6-plugin-selection`、`snapline`、`history` 無法安裝
2. **TypeScript 類型缺失**：v3 Node 類型沒有 `label` 屬性，需用 `node.attr('label/text')` 繞路
3. **節點選擇功能缺失**：沒有 selection 插件，點擊節點無法選中，Backspace 刪除不可用

切換到 `@vue-flow/core` 後上述問題全部解決，且與 Vue 3 生態整合更佳（原生 Vue 組件作為自定義節點、`v-model` 雙向綁定、Composition API）。

### 新增文件清單

| 文件 | 說明 |
|------|------|
| `src/components/VueFlowChart/VueFlowChart.vue` | **通用流程圖組件**，封裝 @vue-flow/core 畫布，支持拖拽新增節點、連線、Backspace 刪除、雙擊編輯、縮放平移、數據導出 |
| `src/components/VueFlowChart/index.ts` | VueFlowChart 組件的導出入口 |
| `src/components/VueFlowChart/nodes/StartNode.vue` | 開始節點（綠色圓角） |
| `src/components/VueFlowChart/nodes/EndNode.vue` | 結束節點（紅色圓角） |
| `src/components/VueFlowChart/nodes/TaskNode.vue` | 任務節點（藍色），顯示工號/姓名/部門代碼 |
| `src/components/VueFlowChart/nodes/ApprovalNode.vue` | 審批節點（橙色），顯示工號/姓名/部門代碼 |
| `src/components/VueFlowChart/nodes/ConditionNode.vue` | 條件分支節點（紫色虛線邊框） |
| `src/views/Business/BusinessView.vue` | 業務安排與尋找主頁面，展示兩個功能入口卡片，組件內切換子頁面 |
| `src/views/Business/PipelineEditor.vue` | 流水線編輯器頁面，整合 VueFlowChart 組件，提供工具欄（保存、適配） |
| `src/views/Business/BusinessOwnerSearch.vue` | 業務負責人搜索頁面，支持防抖搜索、結果卡片展示 |
| `src/api/modules/business.api.ts` | 業務 API 模組（Composable），預留流水線 CRUD + 負責人搜索共 6 個接口 |

### 修改文件清單

| 文件 | 修改內容 |
|------|---------|
| `src/router/index.ts` | 路由從 `quick-contact` → `business`，組件指向 `BusinessView.vue` |
| `config/app-config.json` | 側邊欄 `快速聯繫` → `業務安排與尋找`（圖標改為 DataLine）；浮球菜單同步更新；`quickContact` 配置區塊 → `business` 區塊（新增 pipelineApiEndpoint、ownerSearchApiEndpoint） |
| `src/types/config.types.ts` | `QuickContactConfig` → `BusinessConfig`，包含 pipelineApiEndpoint、ownerSearchApiEndpoint、maxSearchResults |
| `src/types/api.types.ts` | 移除 Contact/SendEmail 等舊類型，新增 FlowNodeData、FlowEdgeData、Pipeline、SavePipelineRequest、BusinessOwner、BusinessOwnerSearchResponse |
| `src/stores/config.store.ts` | getter `contactConfig` → `businessConfig` |

### VueFlowChart 組件 API

**Props：**
| Prop | 類型 | 默認值 | 說明 |
|------|------|--------|------|
| `initialNodes` | `Node[]` | `[]` | 初始節點數據（從後端加載或空數組） |
| `initialEdges` | `Edge[]` | `[]` | 初始邊數據 |

**Events：**
| Event | 說明 |
|-------|------|
| `change` | 圖發生任何變更（新增/刪除/移動/連線/編輯） |

**Methods（通過 ref 調用）：**
| Method | 返回值 | 說明 |
|--------|--------|------|
| `getGraphData()` | `{ nodes, edges }` | 導出當前圖的完整數據（可直接 JSON 序列化） |
| `zoomToFit()` | `void` | 自動縮放適配所有節點 |

**交互操作：**
| 操作 | 說明 |
|------|------|
| 從左側面板拖拽 | 新增對應類型的節點到畫布 |
| 雙擊節點 | 開啟編輯對話框，維護工號、姓名、部門代碼等 |
| 拖拽連接點 | 創建節點之間的連線 |
| 單擊選中 + Backspace | 刪除選中的節點或邊 |
| 滾輪 | 縮放畫布 |

### FlowNodeData 設計

流程圖用於**靜態業務流程留存**，不含動態狀態追蹤欄位：

```typescript
export interface FlowNodeData {
  label: string                  // 節點標題
  nodeType: FlowNodeType         // 節點類型
  employeeId?: string            // 負責人工號（如 'A12345'）
  employeeName?: string          // 負責人姓名
  departmentCode?: string        // 部門代碼（如 'IT-001'）
  description?: string           // 詳細描述
}
```

### 節點編輯功能

雙擊節點可開啟 `el-dialog` 編輯對話框，維護節點的標題、工號、姓名、部門代碼、描述。確認後寫回 `node.data`，Vue Flow 響應式更新畫布。

### 預留 API 接口清單

| 方法 | HTTP 請求 | 說明 |
|------|----------|------|
| `getPipelines()` | GET /pipeline | 獲取流水線列表 |
| `getPipelineById(id)` | GET /pipeline/:id | 獲取單條流水線（含圖數據） |
| `savePipeline(data)` | POST /pipeline | 新增流水線 |
| `updatePipeline(id, data)` | PUT /pipeline/:id | 更新流水線 |
| `deletePipeline(id)` | DELETE /pipeline/:id | 刪除流水線 |
| `searchOwners(keyword)` | GET /owner/search?q= | 搜索業務負責人 |

---

## 清理臨時文件

刪除 `electron.vite.config.1775650378120.mjs`（electron-vite 構建時生成的臨時編譯緩存，因 dev server 異常退出殘留於項目根目錄）。
