# 22 - 公測前重構與瘦身計劃

> **狀態**：線一(代碼品質 A–E)+ 線二(功能瘦身)**均已完成**。這是「公測前」把桌面端瘦身 + 清理代碼的**唯一進度記錄**。
> 之前只散落在代碼註釋與 git `refactor: 重構` 提交裡，一被上下文壓縮就找不到，故立此檔。
> 每完成一項 → 回來勾掉並補一行「證據」（檔案/測試/提交）。

最後更新：2026-07-15

---

## 背景

即將進入**公開測試**。目標兩條線並行：

- **線一 · 代碼品質重構**（不改行為，內部收斂）：分 6 批（A–E），每批 typecheck + test + build 驗證。
- **線二 · 功能瘦身**（面向公測，用戶可見）：清退不需要的功能、刪對應死代碼、把「工作採集」改個所有人都能接受的名字，並清理後端
  TMBOM / 運維前端 TMBOMWEB 中相關無用代碼。

原則：**先摸清現狀拿證據 → 分優先級 → 使用者定範圍與順序 → 逐塊改、每塊驗證行為不變**。不確定是否該刪的，**先問**。

---

## 線一：代碼品質重構（6 批）

### ✅ 批 A — 共享 helper 收斂（已完成）

已在代碼核實並提交（`refactor: 重構`）：

| 產物                                              | 位置                                                       |
|-------------------------------------------------|----------------------------------------------------------|
| `unwrapIpc<T>()`（取代 5 處各寫的 envelope 拆包）         | `src/shared/utils/ipc.ts`                                |
| `BACKEND_BASE_URL`（取代 4 處 `localhost:5247` 字面量） | `src/shared/config/backend.ts`                           |
| `httpClientFor()`（記憶化工廠，取代 4 份粘貼的 lazy 單例）      | `src/api/http-client.ts:131`                             |
| `formatRelative` / `formatStamp`                | `src/shared/utils/format.ts:35 / :47`                    |
| `authStore.userName` getter（取代 3 份 userName 解析） | `src/stores/auth.store.ts:50`                            |
| 單測                                              | `tests/shared/ipc.test.ts`、`tests/shared/format.test.ts` |

驗證：typecheck clean · 145 tests · build ✓。

> 遺留：更大範圍的日期格式化去重（work-collect / project-flow / log-viewer / HomeView 約 20 個檔案手寫 `new Date()`/
`padStart`）**未盲改** —— 格式各不相同，盲換會改變顯示。留到各區域自己的批次逐個核對格式再換。

### ✅ 批 B — 分層修復（已完成 2026-07-15）

`work-analysis.handlers.ts`（原 **391 行**，handler 裡直接跑 LLM 串流 / 校驗 / 寫庫）業務下沉到新檔
`electron/main/work-analysis/runner.ts`，對齊 `preparation.ts` 的功能模組範式。

- 新增 `runner.ts`：`startAnalysisStream(deps, input)`（配額 + slot + 起 stream）、`todayQuota()`、內部 `runStream` / push
  廣播 / countRecords、`DAILY_LIMIT`。經 barrel `work-analysis/index.ts` 導出。
- handler 瘦身到 ~220 行,只剩 payload 校驗 + 委派;`QUOTA`→`todayQuota`、`START_STREAM`→`startAnalysisStream`。
- 驗證:typecheck clean · 131 tests · build ✓。純搬遷,行為不變。

### ✅ 批 C — 拆巨石（已完成 2026-07-15 · 中高風險 · 逐個來）

專案規範是「view/composable(**script**)超 200 行就拆」。**關鍵發現:審計當初按「總行數」排,但大半是 CSS**——
逐檔量 script 後,只有 2 個檔的 script 真正超標,已拆;另 2 個 script 已在規範內,不需動。

| 檔案                                                      | 總行數        | script 行數 | 狀態                         |
|---------------------------------------------------------|------------|-----------|----------------------------|
| `src/windows/agent/App.vue`                             | 1058 → 718 | 421 → ~82 | ✅ 已拆                       |
| `src/features/work-collect/analysis/AnalysisDialog.vue` | 783 → 560  | 303 → ~78 | ✅ 已拆                       |
| `src/views/Home/HomeView.vue`                           | 688        | **153**   | ⏭ script 已 <200,不拆(CSS 撐大) |
| `src/features/work-collect/WorkCollectView.vue`         | 547        | **172**   | ⏭ script 已 <200,不拆(CSS 撐大) |

**AnalysisDialog.vue 拆分結果**（script 303 → ~78 行）：

- `analysis/useAnalysisConfig.ts`:階段 1 配置(時間範圍 / provider / model / 預設 prompt 準備)+ `init()`。
- `analysis/useAnalysisRun.ts`:階段切換 + 流式(StreamingController)+ 報告 + `canStart`/`quotaTooltip`/`parsedReport` +
  `reset()`;`useAnalysisRun(config)` 讀 config 值起 stream。
- SFC 只留 template + 兩個 watch(open→init / close→reset、range→loadPrompts)+ openSettings/handleClose。
- 驗證:typecheck clean · 131 tests · build ✓。行為不變。

**agent/App.vue 拆分結果**（script 421 行 → ~82 行;其餘 478 行為 CSS，未動）：

- `agent/types.ts`：ViewMsg/ChatMsg/ToolMsg/ToolGroup/DisplayItem/PermReq/RawRow。
- `agent/api.ts`：`agentApi` 包裝 electronAPI.agent，envelope 走共用 `unwrapIpc`（順手完成批 A 對 agent 視窗 `call<T>`
  的收斂）。
- `agent/composables/useAgentConversations.ts`：對話列表 / 當前對話 / 工作資料夾 / 歷史懶加載（擁有
  messages、activeConvId、scrollEl）+ 導出 `folderName`。
- `agent/composables/useAgentStream.ts`：送出 / 停止 / 串流事件（deps 注入 conv 的共享 ref）。
- `agent/composables/useAgentPermissions.ts`：權限彈框。
- `agent/composables/useToolGroups.ts`：工具組折疊顯示 + input/output 格式化。
- SFC 只留 template + 薄接線 + CSS。驗證:typecheck clean · 131 tests · build ✓。行為不變。

（`ProjectCanvasView.vue` 已隨功能清退刪除，不在清單。HomeView / WorkCollectView 的 CSS 若要瘦身可留待批 E 或獨立處理，但
script 不需拆。）

### ✅ 批 D — 傳輸統一（已完成 2026-07-15 · 中風險）

- ✅ **主進程 HTTP 去重**:project-flow 與 idea-capture 各自複製的「fetch + 注入 auth + 超時 + 連線層重試一次 + 拆
  envelope」循環,收斂到新檔 `electron/main/services/http/main-http.ts` 的 `sendMain()`。
  - http-utils 從 `services/project-flow/` 移到中性的 `services/http/`(純函式,`git mv`;測試 import 同步更新)。
  - 兩個 api-client 只剩「端點 → init(JSON / multipart)」薄映射;各自保留原 header 行為(header 一字未變 → 行為不變)。
  - project-flow 的 userId query 注入、idea-capture 的 multipart create,都照舊。
- ✅ **策略文檔化**:在 `src/api/http-client.ts` 頭部寫清兩條通道(① 渲染端 axios 直連=主要;② 主進程
  fetch=需主進程上下文者),
  並**刪掉原本「主進程不做任何 HTTP」的錯誤註解**。新功能預設走通道 ①。
- ⏸ **Dify 密鑰**:`useRepairPolish.ts:30` 把 `VITE_DIFY_API_KEY` 內聯進渲染端 bundle。**使用者決定不處理**——
  與「內網信任邊界、API key 明文為有意設計」的既有取態一致,不視為待修項。
- 驗證:typecheck clean · 131 tests(含移位後的 http-utils 測試)· build ✓。

### ✅ 批 E — 註釋瘦身（已完成 2026-07-15 · 低風險）

砍掉最大宗的一類考古註釋:**i18n 遷移遺留的「原文…:」**(記錄被 i18n key 取代前的舊硬編碼中文,i18n key + zh-TW.json
已是權威,純噪音)。

- 兩遍腳本 + 一處手動,共清 **~113 處、跨 ~24 檔**;整行考古刪整行,「有用註釋;原文:xxx」只刪後綴。
- **明確保留合法用法**(原文=「原始文本」概念,非考古):想法詳情的「原文/AI版」切換、repair prompt「與原文保持一致」、agent 工具
  `describe('要被替換的原文…')`、UnifiedPlatform「JSON 原文」搜尋說明、auth.interceptor 作者注明「保留方便排查」的原文映射。
- 驗證:typecheck clean · 131 tests · build ✓(build 編譯模板 → 保證沒刪壞註釋語法)。
- 其餘「版本敘事(v2 設計 / 階段二)」類註釋多半仍夾帶「為什麼」,誤刪風險高、收益低,**刻意不做過度清掃**。

---

## 線二：功能瘦身（公測前）

### 個人功能：只留「工作採集 + 備忘錄」

現狀（`electron/main/db/features/config/defaults.ts` personalFunctions.tools）：已只剩
`workCollect`（顯示名「工作回顧」）+ `projectMemos`（「備忘錄」）→ **目標已達成**。
待辦：確認別處沒有殘留其他個人工具入口。

### 工作採集改名

✅ **使用者已自行處理**（2026-07-15 告知「改名已經好了，不用管」）。此項不再由重構流程觸碰。

### project-flow 命名遺留（可選）

專案/畫布/匯報/反饋/團隊已清退。殘留的 `project-flow` 代碼**不是死代碼** —— 已被複用為
**備忘錄 + 首頁儀表板**的後端管道（memos CRUD、AI 建議、今日活動、我的節點）。
可選清理：把 `project-flow` 這個誤導性命名改成 `memos` / `dashboard`（純改名，風險低但面廣）。

### 後端 TMBOM / 運維前端 TMBOMWEB 清理（已深查 2026-07-15）

**結論：被清退功能的死代碼「早前已基本刪乾淨」，兩庫都沒有實質可刪的東西。**

- **TMBOMWEB（運維前端）**：views 無任何 project-flow / 專案·畫布·匯報·反饋·團隊 頁面；grep 命中的「匯報 / memo」全是 IT
  報修與快遞欄位（保留功能）。→ **無需改動。**
- **TMBOM（後端）**：project-flow 只剩
    - `ProjectFlowController.cs` → 只有 `my-nodes` + 5 個 memo 端點
    - `IProjectFlowService` / `ProjectFlowService.cs`(72行，`ListMyNodesAsync` 已是**回空列表的空殼**) /
      `ProjectFlowService.Memos.cs`
    - Model：`ProjectFlowQueries/Requests/Responses` 只含 `Memo*` + `MyNodeItem`
    - 專案/節點/連線/匯報/反饋/團隊 的 Entity/Repository **已刪除**（找不到）
  - → memos 目前是保留功能,**暫不刪**。⚠️ 但備忘錄正改造成**本地代辦**(docs/23),上線後後端 memos 全套即無用 ——
    **退場清單見 [docs/23 §10]**(Controller/Service/DTO/Repository/Entity + `Project_Memos` 表),屆時再刪。

**兩個可選清理的處置：**

1. ✅ **my-nodes 空轉鏈 —— 已整條拆除（2026-07-15）**。移除範圍：
    - 桌面：`MemosView.vue` onAiSuggest 用法、`features/project-flow/{api.ts,types.ts}`、`types/electron/project-flow.ts`、
      `ipc-handlers/project-flow.handlers.ts`、`services/project-flow/api-client.ts`、
      `preload/bridges/project-flow.bridge.ts`、`preload/memos.preload.ts`、`shared/ipc-channels/project-flow.ts`、
      `shared/types/project-flow.types.ts`(MyNodeItem)、`services/project-flow/ai-local.ts`(MemoSuggestInput.nodes +
      prompt 去節點)。
    - 後端：`ProjectFlowController`(ListMyNodes + 連帶死掉的 RunAsync)、`IProjectFlowService`、`ProjectFlowService.cs`、
      `ProjectFlowResponses.cs`(MyNodeItem)。
    - 驗證：桌面 typecheck clean · 131 tests · build ✓；後端 `dotnet build` 0 errors。AI 備忘建議行為不變（nodes 本就恆空）。
2. ⏸ **`project-flow` 命名遺留 —— 使用者決定暫不改**。保持現名。

**不動**：`sql-migrations/20260610_ProjectFlow_Members_And_Indexes.sql` 等遷移檔是歷史，不刪。
保護清單（勿刪）：memos、work-collect、idea-capture、IT 報修、BPM、AiSop、auth、使用者同步、遠程通知 SignalR、每日建議、自動更新。

---

## 進度日誌

- 2026-07-15：批 A 完成並驗證（見上）。建立本記錄檔。線二個人功能瘦身確認目標已達成；工作採集改名待定；後端/運維前端清理待深查。
- 2026-07-15：使用者定調——改名已自理（不動）；下一步只讀深查 TMBOM + TMBOMWEB，出死代碼刪除清單給使用者審，**先不刪**。
- 2026-07-15：深查完成。結論：兩庫的清退死代碼早前已基本刪淨，無實質可刪。只剩兩個可選清理待使用者定（my-nodes
  空轉鏈、project-flow 命名遺留）。
- 2026-07-15：使用者定 → my-nodes 整條拆除（桌面 + 後端，已驗證）；project-flow 命名暫不改。**線二功能瘦身至此收尾。**
- 2026-07-15：批 B 完成——work-analysis.handlers.ts 業務下沉 runner.ts,handler 391→~220 行,驗證通過。Dify 密鑰項使用者決定不處理。
- 2026-07-15：批 C 起步——agent/App.vue 1058→718 行,script 拆成 types/api + 4 composable,驗證通過。剩 AnalysisDialog /
  HomeView / WorkCollectView 待拆。
- 2026-07-15：批 C 完成——AnalysisDialog 303→~78 行 script,拆 useAnalysisConfig + useAnalysisRun,驗證通過。量測後確認
  HomeView(153)/WorkCollectView(172)的 script 已 <200,不需拆(大行數是 CSS)。**批 C 收尾。** 線一剩批 D(傳輸統一)、批 E(
  註釋瘦身)。
- 2026-07-15：批 D 完成——主進程 HTTP 去重成 services/http/main-http.ts 的 sendMain(project-flow + idea-capture 共用),
  http-utils 移到 services/http/,http-client.ts 補上雙通道策略註解並刪掉「主進程不發 HTTP」的假話。驗證通過。線一剩批 E(
  註釋瘦身)。
- 2026-07-15：批 E 完成——清 ~113 處 i18n「原文:」考古註釋(跨 ~24 檔),保留合法用法,驗證通過。**線一(A–E)全部完成;整個公測前重構與瘦身計劃收尾。
  **
