# 本地數據庫(SQLite + Drizzle ORM)設計 —— 日誌系統增強

> **狀態:設計已定案,等待動工。**
>
> **本期範圍**:在現有「按日寫純文字 `.log` 檔」之上**並行新增**寫入 SQLite。txt 檔行為**完全不變**,SQLite 視為**新增的第二寫入端**,而非取代。
> 同時建立 DB 基礎建設(better-sqlite3 + drizzle-orm + drizzle-kit + migration 流程),為後續其他模組鋪好路 —— 但**那些模組不在本期範圍**。
>
> **本期不做使用者 UI**。SQLite 資料定位是**開發 / 排查用內部資源**,不曝露給終端使用者。如未來要加查詢面板,以「條件式顯示」(dev 模式 / 隱藏快捷鍵解鎖)為前提另案規劃。
>
> 目標環境:Windows 桌面客戶端(prod userData 為 `%APPDATA%\ichiaDesktop\`,dev 為專案根 `logs/`)。

## 本期已定案的 5 項關鍵決議

> 寫在最前面,後面各章節已按此落實。

1. **渲染端 forward 範圍** —— `src/utils/logger.ts` 內的 `forwardToMain` 從「只 ERROR」改為「所有等級」。讓 DB 拿得到完整渲染端上下文(error 前後的 info / debug)。詳見 §12.2。
2. **DB 保留天數 = 14** —— 跟現有 `log-file-writer` 的 `RETENTION_DAYS` 對齊;啟動時 `cleanupOlderThan(14)`。詳見 §14.1。
3. **DB 檔路徑** —— dev 在**專案根**(`<projectRoot>/app.db`),prod 在 **`<userData>/app.db`**。跟現有 txt 檔的 dev/prod 路徑分流規則完全一致。詳見 §9。
4. **DB init 失敗 → 靜默降級** —— 只 `console.error` 報出,**不 `attachLogService`**;主進程 logger 內 `_logService?.write(...)` 自動 noop;txt 寫入仍正常;App 照常啟動。詳見 §17.1。
5. **本期不加任何渲染端可呼叫的 DB IPC,不加 UI** —— 寫入端沿用既有 `LOG_WRITE`,查詢端不曝露;`LogService.query()` 仍實作但只在主進程內部可用,留給未來 dev-only 面板。詳見 §13、§18。

---

## 目錄

1. [本期範圍與目標](#1-本期範圍與目標)
2. [現狀盤點(以實際代碼為準)](#2-現狀盤點以實際代碼為準)
3. [雙寫策略](#3-雙寫策略)
4. [技術棧結論](#4-技術棧結論)
5. [整體架構](#5-整體架構)
6. [檔案組織](#6-檔案組織)
7. [logs 表 Schema 設計](#7-logs-表-schema-設計)
8. [Drizzle Schema 寫法](#8-drizzle-schema-寫法)
9. [DatabaseManager 主進程單例](#9-databasemanager-主進程單例)
10. [Migration 工具鏈](#10-migration-工具鏈)
11. [LogService 設計](#11-logservice-設計)
12. [整合現有 logger.ts(主進程 + 渲染)](#12-整合現有-loggerts主進程--渲染)
13. [IPC 設計](#13-ipc-設計)
14. [保留策略與清理](#14-保留策略與清理)
15. [打包配置](#15-打包配置)
16. [啟動順序](#16-啟動順序)
17. [錯誤處理與降級](#17-錯誤處理與降級)
18. [未來查詢能力的延伸位置](#18-未來查詢能力的延伸位置)
19. [落地步驟](#19-落地步驟)
20. [影響檔案清單](#20-影響檔案清單)

---

## 1. 本期範圍與目標

### 1.1 要做

- 引入 `better-sqlite3 + drizzle-orm + drizzle-kit` 基礎建設
- 設計 `logs` 表 schema,主 / 渲染兩端的日誌**同時**寫進 DB
- 改造主進程 logger:寫 txt 的同時也寫 DB
- 改造渲染端入口(log handler):收 LOG_WRITE 時同時寫 txt 與 DB
- 自動清理:啟動時刪除 N 天前的舊紀錄
- 為後續其他表 / 模組保留延伸位置(DatabaseManager 設計成可擴的)

### 1.2 不做

- **任何使用者面向的查詢 UI**(SQLite 是內部排查資源,不曝露給終端使用者)
- 其他資料表(通知、最近紀錄、審計等),全部留給後續
- 改變 logger 對 caller 的 API 簽名

### 1.3 成功標準

- 任何呼叫 `logger.*` 的地方,**業務代碼零修改**仍照舊運作
- 寫入 txt 的行為跟原本完全一致(同樣的格式、同樣的檔案命名、同樣只 ERROR 落地)
- **同一筆日誌(所有等級)同時進 SQLite 的 `logs` 表**,可日後查詢
- 啟動時 DB 失敗 → txt 仍正常寫入,App 正常啟動,DB 寫入端口靜默 noop
- DB 檔自動清理 N 天前的舊紀錄

---

## 2. 現狀盤點(以實際代碼為準)

> 此節依據**實際代碼**而非 [docs/07](./07-日志系统重构设计.md)。07 是更早的重構提案,部分內容已落地、部分未落地,本文不再參考它,改直接對齊代碼。

### 2.1 實際代碼

| 模塊 | 檔案 | 實際行為 |
|---|---|---|
| 主進程 logger | [electron/main/utils/logger.ts](../electron/main/utils/logger.ts) | `debug / info / warn` 只走 console;**只有 `error` 會呼叫 `writeLine('main', ...)` 落地** |
| 渲染端轉發入口 | 同上 `writeRendererLog()` | dev 鏡像到主進程 console;**只有 `ERROR` 才 `writeLine('renderer', ...)`** |
| 文件寫入器 | [electron/main/utils/log-file-writer.ts](../electron/main/utils/log-file-writer.ts) | `appendFile` 異步追加;**14 天保留**;dev 路徑為專案根 `logs/`,prod 為 `<userData>/logs/`;100/秒節流由 ipc handler 做 |
| 渲染進程 logger | [src/utils/logger.ts](../src/utils/logger.ts) | API 跟主進程一致;`debug / info / warn` 只走 console;`error` 額外 `forwardToMain()` 走 IPC |
| IPC | [electron/main/ipc-handlers/log.handlers.ts](../electron/main/ipc-handlers/log.handlers.ts) | `LOG_WRITE` (`on`,單向);`LOG_OPEN_FOLDER` (`invoke`);**100/秒節流在這裡實作** |
| 設定 UI | [src/views/Settings/sections/LogSection.vue](../src/views/Settings/sections/LogSection.vue) | 顯示路徑、打開資料夾、複製路徑 |

### 2.2 重要事實(本期設計依此而非 07 的描述)

1. **目前實際只有 ERROR 等級會落到 txt 檔**。其他等級只在 console 出現,關掉 App 就沒了。
2. **節流 100/秒由 ipc handler 實作**。本期 LogService 寫入端如果也要節流要評估,目前傾向**不重複加節流**(複用 handler 既有的)。
3. **dev 模式 txt 檔在專案根 `logs/`**,不是 userData。寫 DB 時也應該分 dev / prod 處理路徑。
4. **檔名按來源 + 日期**:`main-2026-05-15.log` / `renderer-2026-05-15.log`。
5. 主進程 `logger.error` 跟 `writeRendererLog('ERROR', ...)` 的最終寫檔都走 `writeLine()`,**這一條路徑現在要分叉成「寫 txt + 寫 DB」**。

---

## 3. 雙寫策略

### 3.1 為什麼雙寫

| 端 | 角色 | 為什麼留 |
|---|---|---|
| **txt 檔** | 給支援人員 / IT 拿走的「物理工件」 | 就算 DB 損壞,txt 也在;ZIP 壓給支援打包簡單;熟悉的工作流程不破壞 |
| **SQLite** | 開發 / 排查時用結構化查詢 | 跨日跨進程關聯查詢、按 level / module 過濾、按關鍵字搜尋,純文字做不到 |

兩者**獨立寫入**:其中一端失敗不影響另一端。

### 3.2 兩端寫入規則

| 等級 | txt 檔 | SQLite |
|---|---|---|
| debug | ✗(維持現狀) | ✓ |
| info  | ✗(維持現狀) | ✓ |
| warn  | ✗(維持現狀) | ✓ |
| error | ✓(維持現狀) | ✓ |

理由:

- **txt 維持現狀只 ERROR**:支援人員從使用者拿到 txt 時,只關心錯誤;塞 debug/info 進去反而混淆
- **DB 寫所有等級**:儲存便宜、索引讓查詢便宜、開發排查 deep dive 時要看上下文(error 前後的 info / debug)

### 3.3 寫入時機

- 主進程 `logger.*` 內呼叫:**同步** `logService.write(...)` + 原本邏輯(只 ERROR 寫 txt)
- 渲染端透過 `LOG_WRITE` IPC 進來,handler 內:**同步** `logService.write(...)` + 原本邏輯(只 ERROR 寫 txt)

兩端走同一個 `LogService.write()` 入口,只是 `source` 欄位不同。

---

## 4. 技術棧結論

| 層 | 套件 | 為什麼 |
|---|---|---|
| 驅動 | `better-sqlite3` | Electron 主進程同步 API 最快;npm 預編譯 binary,跨平台無痛;`@types/better-sqlite3` 型別完整 |
| ORM / Query Builder | `drizzle-orm` | 純 TS 寫 schema 自動推導型別,query builder 寫起來像 SQL,沒有獨立 engine binary,Electron 打包友善 |
| Migration 工具 | `drizzle-kit` (devDep) | 比較 schema 變更自動產 SQL migration |
| 啟動跑 migration | `drizzle-orm/better-sqlite3/migrator` | App 啟動內建 |

排除選項(Prisma / TypeORM / Kysely)前一版討論過,不重複。

---

## 5. 整體架構

```
┌──────────────────────────────────────────────────────────────────┐
│ Renderer (src/)                                                  │
│   業務 → src/utils/logger.ts (API 不變)                           │
│                ├─► console.* (所有等級)                           │
│                └─► (僅 ERROR) window.electronAPI.log.write(...)   │
└──────────────────────────────────────────────────────────────────┘
                       │ LOG_WRITE (send)
                       ▼
┌──────────────────────────────────────────────────────────────────┐
│ Main (electron/main/)                                            │
│                                                                  │
│  ipc-handlers/log.handlers.ts (改寫)                              │
│    ├─► writeRendererLog(...) (原邏輯,寫 txt;只 ERROR)             │
│    └─► logService.write({source:'renderer', ...})  ← 新增         │
│                                                                  │
│  utils/logger.ts (改寫;對主進程內呼叫)                            │
│    ├─► writeLine(...) (原邏輯,寫 txt;只 ERROR)                    │
│    └─► logService.write({source:'main', ...})  ← 新增,所有等級    │
│                                                                  │
│  db/services/log.service.ts                                      │
│    └─► drizzle insert into logs                                  │
│                                                                  │
│  db/database-manager.ts ← 持有 sqlite + drizzle                  │
│  db/schema/logs.ts                                               │
│  db/migrations/*.sql                                             │
│                                                                  │
│  utils/log-file-writer.ts ← **完全保留,不動**                     │
│                                                                  │
│  <userData>/                                                     │
│    ├─ logs/main-2026-05-15.log                                   │
│    ├─ logs/renderer-2026-05-15.log                               │
│    └─ app.db   ← 新增,SQLite 檔                                   │
└──────────────────────────────────────────────────────────────────┘
```

**設計原則**:

- SQLite 只活在主進程,渲染端零引用
- LogService 是業務 API 入口,IPC handler 只做拆參 / 呼叫
- **既有的 log-file-writer.ts 完全不動**,呼叫點也完全不動 —— 雙寫的兩條路徑互不干涉

---

## 6. 檔案組織

```
electron/
├── main/
│   ├── db/                              # 新增整個資料夾
│   │   ├── database-manager.ts          # sqlite + drizzle 單例
│   │   ├── schema/
│   │   │   ├── index.ts                 # re-export
│   │   │   └── logs.ts                  # 本期唯一一張表
│   │   ├── services/
│   │   │   └── log.service.ts           # write / cleanup
│   │   └── migrations/                  # drizzle-kit 自動產出
│   │       ├── 0000_initial.sql
│   │       └── meta/...
│   ├── ipc-handlers/
│   │   └── log.handlers.ts              # 改:在原寫 txt 之外加寫 DB
│   └── utils/
│       ├── logger.ts                    # 改:在原寫 txt 之外加寫 DB
│       └── log-file-writer.ts           # **不動**
src/                                     # 渲染端完全不動
drizzle.config.ts                        # 新增
```

---

## 7. logs 表 Schema 設計

### 7.1 欄位

| 欄位 | 型別 | 必填 | 說明 |
|---|---|---|---|
| `id` | INTEGER PK AUTOINCREMENT | ✓ | — |
| `createdAt` | INTEGER(Unix ms) | ✓ | 查詢主軸,索引 |
| `level` | TEXT(`debug` / `info` / `warn` / `error`) | ✓ | 跟現有 logger 對齊,小寫 |
| `source` | TEXT(`main` / `renderer`) | ✓ | 來源進程 |
| `module` | TEXT | optional | logger 第 2 參數 |
| `message` | TEXT | ✓ | 主訊息 |
| `args` | TEXT(JSON) | optional | 第 3+ 參數 `JSON.stringify` |
| `errorStack` | TEXT | optional | args 含 Error 時抽出 stack,便於搜尋 |

### 7.2 索引

- `idx_logs_createdAt`:列「最近 N 筆」用
- `idx_logs_level_createdAt`:按 level 過濾用

`module` 索引等實際看使用模式再加。

### 7.3 為什麼不全部塞欄位(例如把每個 arg 展開)

- args 是不定型,塞 JSON 最通用
- `errorStack` 例外拉出來,因為**「找 stack 含 X」**是高頻排查動作,做不到 JSON 內檢索

---

## 8. Drizzle Schema 寫法

`electron/main/db/schema/logs.ts`:

```ts
import {sqliteTable, integer, text, index} from 'drizzle-orm/sqlite-core'

export type LogLevel = 'debug' | 'info' | 'warn' | 'error'
export type LogSource = 'main' | 'renderer'

export const logs = sqliteTable(
    'logs',
    {
      id: integer('id').primaryKey({autoIncrement: true}),
      createdAt: integer('createdAt').notNull(),
      level: text('level').$type<LogLevel>().notNull(),
      source: text('source').$type<LogSource>().notNull(),
      module: text('module'),
      message: text('message').notNull(),
      args: text('args'),
      errorStack: text('errorStack')
    },
    (table) => ({
      idxCreatedAt: index('idx_logs_createdAt').on(table.createdAt),
      idxLevelCreated: index('idx_logs_level_createdAt')
          .on(table.level, table.createdAt)
    })
)

export type LogRow = typeof logs.$inferSelect
export type NewLog = typeof logs.$inferInsert
```

---

## 9. DatabaseManager 主進程單例

`electron/main/db/database-manager.ts`:

```ts
import Database from 'better-sqlite3'
import {drizzle, BetterSQLite3Database} from 'drizzle-orm/better-sqlite3'
import {migrate} from 'drizzle-orm/better-sqlite3/migrator'
import {app} from 'electron'
import path from 'path'
import * as schema from './schema'

export class DatabaseManager {
  private sqlite: Database.Database | null = null
  private db: BetterSQLite3Database<typeof schema> | null = null

  /**
   * 啟動順序見 §16。
   * 內部不呼叫 logger(避免循環依賴 —— logger 反過來要呼叫本類);
   * 失敗用原生 console.error 報出後 rethrow,由 index.ts 決定降級。
   */
  init(): void {
    // dev 時跟 txt 一致,放專案根 db/;prod 放 userData
    const baseDir = app.isPackaged ? app.getPath('userData') : app.getAppPath()
    const dbPath = path.join(baseDir, 'app.db')
    const migrationsFolder = path.join(__dirname, 'migrations')

    this.sqlite = new Database(dbPath)
    this.sqlite.pragma('journal_mode = WAL')
    this.sqlite.pragma('foreign_keys = ON')

    this.db = drizzle(this.sqlite, {schema})
    migrate(this.db, {migrationsFolder})
  }

  getDb(): BetterSQLite3Database<typeof schema> {
    if (!this.db) throw new Error('DatabaseManager.init() 尚未呼叫')
    return this.db
  }

  isReady(): boolean { return this.db !== null }

  close(): void {
    this.sqlite?.close()
    this.sqlite = null
    this.db = null
  }
}
```

**為什麼 dev 跟 prod 路徑不同**(本期決議 #3):跟現有 `log-file-writer` 行為對齊 —— dev 在專案根(`<projectRoot>/app.db`),直接看資料、清資料方便;prod 在 `<userData>/app.db`,規範安裝路徑。

---

## 10. Migration 工具鏈

### 10.1 `drizzle.config.ts`(專案根)

```ts
import {defineConfig} from 'drizzle-kit'

export default defineConfig({
  schema: './electron/main/db/schema/index.ts',
  out: './electron/main/db/migrations',
  dialect: 'sqlite',
  verbose: true,
  strict: true
})
```

### 10.2 npm scripts(加在 [package.json](../package.json))

```json
{
  "scripts": {
    "db:generate": "drizzle-kit generate",
    "db:check":    "drizzle-kit check"
  }
}
```

### 10.3 流程

1. 改 `schema/*.ts`
2. `npm run db:generate` → 產出 `NNNN_<auto>.sql` + 更新 `meta/_journal.json`
3. review SQL → commit
4. App 啟動 `migrate()` 自動套用

第一次跑會產出 `0000_initial.sql`(`CREATE TABLE logs` + 索引)。必 commit。

---

## 11. LogService 設計

`electron/main/db/services/log.service.ts`:

```ts
import {lt, desc, type SQL, and, eq, gte, like, inArray} from 'drizzle-orm'
import type {DatabaseManager} from '../database-manager'
import {logs, type LogRow, type LogLevel, type LogSource} from '../schema/logs'

export interface LogQueryParams {
  level?: LogLevel | LogLevel[]
  source?: LogSource
  module?: string
  since?: number
  until?: number
  search?: string
  limit?: number
  offset?: number
}

export class LogService {
  constructor(private readonly dbManager: DatabaseManager) {}
  private get db() { return this.dbManager.getDb() }

  /**
   * 寫一筆。logger / handler 直接呼叫。
   * 內部 try / catch:寫失敗只報 console,絕不擴散(避免日誌失敗炸業務)。
   * dbManager 未 ready 時 noop —— 啟動順序保險(極早期日誌可能落空,可接受)。
   */
  write(entry: {
    level: LogLevel
    source: LogSource
    module?: string
    message: string
    args?: unknown[]
  }): void {
    if (!this.dbManager.isReady()) return
    try {
      const {argsJson, errorStack} = serializeArgs(entry.args)
      this.db.insert(logs).values({
        createdAt: Date.now(),
        level: entry.level,
        source: entry.source,
        module: entry.module ?? null,
        message: entry.message,
        args: argsJson,
        errorStack
      }).run()
    } catch (err) {
      console.error('[LogService] DB 寫入失敗', err)
    }
  }

  /** 預留:給未來 dev-only 查詢面板使用 */
  query(params: LogQueryParams): LogRow[] {
    const conds: SQL[] = []
    if (params.level) {
      conds.push(Array.isArray(params.level)
          ? inArray(logs.level, params.level)
          : eq(logs.level, params.level))
    }
    if (params.source) conds.push(eq(logs.source, params.source))
    if (params.module) conds.push(eq(logs.module, params.module))
    if (params.since != null) conds.push(gte(logs.createdAt, params.since))
    if (params.until != null) conds.push(lt(logs.createdAt, params.until))
    if (params.search) conds.push(like(logs.message, `%${params.search}%`))

    return this.db.select().from(logs)
        .where(conds.length ? and(...conds) : undefined)
        .orderBy(desc(logs.createdAt))
        .limit(params.limit ?? 200)
        .offset(params.offset ?? 0)
        .all()
  }

  /** 啟動時清理 N 天前舊紀錄 */
  cleanupOlderThan(days: number): number {
    if (!this.dbManager.isReady()) return 0
    const cutoff = Date.now() - days * 24 * 60 * 60 * 1000
    const result = this.db.delete(logs).where(lt(logs.createdAt, cutoff)).run()
    return result.changes
  }
}

function serializeArgs(args?: unknown[]): {argsJson: string | null; errorStack: string | null} {
  if (!args || args.length === 0) return {argsJson: null, errorStack: null}

  let errorStack: string | null = null
  const serialized = args.map((a) => {
    // 渲染端來的 Error 已經被 sanitizeArg 轉成 {__error__, name, message, stack}
    if (a instanceof Error) {
      errorStack = errorStack ?? a.stack ?? null
      return {name: a.name, message: a.message, stack: a.stack}
    }
    if (a && typeof a === 'object' && (a as {__error__?: boolean}).__error__) {
      const e = a as {stack?: string}
      errorStack = errorStack ?? e.stack ?? null
    }
    return a
  })
  return {argsJson: JSON.stringify(serialized), errorStack}
}
```

**注意 `query` / `cleanupOlderThan` 是預留 API**,本期本期不曝露給 renderer,只在主進程內部用(cleanup 由啟動鉤子呼叫)。未來要做 dev-only 面板時再加 IPC channel。

---

## 12. 整合現有 logger.ts(主進程 + 渲染)

### 12.1 主進程 logger.ts 改造重點

**目標**:每個 level 都呼叫 `logService.write(...)`,**同時**保留現有寫 txt 的邏輯(只 ERROR)。

```ts
// electron/main/utils/logger.ts
import {writeLine} from './log-file-writer'
import type {LogService} from '../db/services/log.service'

let _logService: LogService | null = null

/** index.ts 在 DatabaseManager.init() 後呼叫,把 service 注入。失敗時不呼叫 → DB 寫入靜默 noop */
export function attachLogService(svc: LogService): void {
  _logService = svc
}

export const logger = {
  debug(message: string, module?: string, ...args: unknown[]): void {
    if (process.env.NODE_ENV !== 'production') {
      console.debug(formatLine('DEBUG', message, module, args))
    }
    _logService?.write({level: 'debug', source: 'main', module, message, args})
  },
  info(message: string, module?: string, ...args: unknown[]): void {
    console.info(formatLine('INFO', message, module, args))
    _logService?.write({level: 'info', source: 'main', module, message, args})
  },
  warn(message: string, module?: string, ...args: unknown[]): void {
    console.warn(formatLine('WARN', message, module, args))
    _logService?.write({level: 'warn', source: 'main', module, message, args})
  },
  error(message: string, module?: string, ...args: unknown[]): void {
    const line = formatLine('ERROR', message, module, args)
    console.error(line)
    writeLine('main', line)                                       // ← 既有,不動
    _logService?.write({level: 'error', source: 'main', module, message, args})
  }
}

// writeRendererLog 同樣加一行 _logService?.write({source:'renderer', ...})
```

### 12.2 渲染端 logger.ts(`src/utils/logger.ts`)

**對外 API 完全不變**。內部仍走 `window.electronAPI.log.write(...)`,IPC payload 形狀也不變。**渲染端業務代碼零修改**。

**本期決議:`forwardToMain` 從「只 ERROR」改為「所有等級」**(對應本期決議 #1)。

理由:

- 排查問題時最缺的就是「錯誤前的渲染端上下文」(error 前面的 info / debug),DB 拿到完整時間線才有價值
- 100/秒節流(由 [log.handlers.ts](../electron/main/ipc-handlers/log.handlers.ts) 既有實作)足以擋住失控的流量,不需要動
- 影響面只在 `src/utils/logger.ts` 內部一行邏輯,業務 caller 不感知

對應修改點:`src/utils/logger.ts` 的 `debug` / `info` / `warn` 三個方法,在原本只 `console.*` 之外都加上 `forwardToMain(...)`。`error` 維持現狀(已經 forward)。

### 12.3 渲染端 handler 改造

`electron/main/ipc-handlers/log.handlers.ts` 內:

```ts
ipcMain.on(IpcChannels.LOG_WRITE, (_event, entry) => {
  // ...既有節流 + writeRendererLog (只 ERROR 寫 txt) 完全保留
  writeRendererLog(entry.level ?? 'INFO', entry.message, entry.module, entry.args)

  // 新增:同步寫 DB
  logService.write({
    level: entry.level.toLowerCase() as LogLevel,    // renderer 用大寫,DB 統一小寫
    source: 'renderer',
    module: entry.module,
    message: entry.message,
    args: entry.args
  })
})
```

---

## 13. IPC 設計(本期決議 #5)

本期**不新增任何渲染端可呼叫的 DB IPC channel**。理由:

- 寫入端沿用既有的 `LOG_WRITE`(渲染端不知道後端多了個 DB 寫入)
- 查詢端不曝露給渲染端(沒有 UI 要用)
- 未來真要加 dev-only 面板,再加 `LOG_QUERY` / `LOG_CLEAR` channel

`LOG_OPEN_FOLDER` 維持原狀(打開 txt 資料夾)。

---

## 14. 保留策略與清理

### 14.1 自動清理(本期決議 #2)

App 啟動時呼叫 `logService.cleanupOlderThan(14)`,跟 [log-file-writer.ts](../electron/main/utils/log-file-writer.ts) 的 `RETENTION_DAYS = 14` **對齊**。

實作上,本期**保持「兩處同樣寫 14」的硬編碼**,不抽到 config —— 簡單最重要;真要統一,後續任務再把 14 抽到 `app-config.json` 並讓兩端讀同一份。

### 14.2 大小封頂 / VACUUM

本期不做,等實際觀測 DB 檔大小再加。

---

## 15. 打包配置

### 15.1 必加:`asarUnpack`

[package.json](../package.json) `build` 區塊:

```json
"asarUnpack": [
  "**/node_modules/better-sqlite3/**"
]
```

better-sqlite3 的 native `.node` 必須以實體檔形式存在於檔案系統。

### 15.2 已具備:`postinstall: install-app-deps`

[package.json:18](../package.json) 已有,自動重編 native module 為 Electron Node ABI。

### 15.3 把 migrations SQL 帶進打包

`electron.vite.config.ts` 加 `vite-plugin-static-copy`:

```ts
viteStaticCopy({
  targets: [{src: 'electron/main/db/migrations/*', dest: 'migrations'}]
})
```

確保 `out/main/migrations/` 有 SQL 檔。

---

## 16. 啟動順序

`electron/main/index.ts` 修改要點:

```ts
let dbManager: DatabaseManager
let logService: LogService

app.whenReady().then(async () => {
  // ── 既有 ──
  initLogFileWriter()
  app.setAppUserModelId('com.ichia.desktop.client')
  ensureAutoLaunchRegistered()

  // ── 新增:DatabaseManager + LogService ──
  // 包 try/catch,失敗只報 console,let App 繼續啟動(txt 仍正常)
  try {
    dbManager = new DatabaseManager()
    dbManager.init()
    logService = new LogService(dbManager)
    attachLogService(logService)             // 主進程 logger 注入
    logService.cleanupOlderThan(14)
  } catch (err) {
    console.error('[App] DB 初始化失敗,日誌只走 txt', err)
  }

  // ── 既有 ──
  configManager = new ConfigManager()
  await configManager.load()
  // ...

  // 註冊 handler 時傳入 logService(若 null 則 handler 內 noop)
  registerAllHandlers(windowManager, configManager, floatingBallMgr, updateMgr, logService)

  // ...
})

function gracefulShutdown(): void {
  // ...既有
  dbManager?.close()    // 確保 WAL 落盤
  // ...
}
```

**順序重點**:

1. `initLogFileWriter()` 必須最先,因為下面如果 DB init 失敗會用 console.error,但即便 console 不到位 file writer 不依賴 logger
2. DatabaseManager.init() **必須在 logger 任何寫入之前**完成,否則早期日誌寫不進 DB(可接受 —— 早期失敗本來就罕見,且 txt 仍會寫到)
3. logService 注入 logger 後,後續所有主進程 logger 呼叫都會雙寫

---

## 17. 錯誤處理與降級

### 17.1 DB init 失敗(本期決議 #4)

`dbManager.init()` 拋例外 → index.ts catch 住 → **不 `attachLogService`** → 主進程 logger 內 `_logService?.write(...)` 自動 noop → **txt 寫入仍正常**。

App **照常啟動**,使用者完全不感知。只有開發者在主進程 console 能看到 `[App] DB 初始化失敗,日誌只走 txt` 的錯誤輸出。

### 17.2 寫入失敗

LogService.write 內 try/catch,失敗只 console.error。**logger 呼叫永遠不拋**,業務代碼可以放心使用。

### 17.3 DB 損毀

`migrate()` 失敗會拋,落到 17.1 路徑。使用者完全不感知,只在主進程 console 可見錯誤。

緊急時手動操作:刪 `<userData>/app.db` + `app.db-wal` + `app.db-shm`,下次啟動自動重建。

---

## 18. 未來查詢能力的延伸位置

本期不做 UI,但留好以下擴充點:

1. **LogService.query()** 已實作,直接可用
2. 加新 IPC:`LOG_QUERY` / `LOG_CLEAR`(本期不加,需要時再加)
3. dev-only 面板可放在 Settings 頁的隱藏分頁,觸發條件待定(例如版本號連點 5 次解鎖)
4. 或做成獨立子視窗 `log-viewer.html`,從主進程 menu 觸發

這些都不在本期範圍。

---

## 19. 落地步驟

每步可獨立 commit:

1. **裝相依**
   ```bash
   npm i better-sqlite3 drizzle-orm --legacy-peer-deps
   npm i -D @types/better-sqlite3 drizzle-kit vite-plugin-static-copy --legacy-peer-deps
   ```

2. **`package.json`**:加 `asarUnpack` + db scripts

3. **建 `drizzle.config.ts`**(專案根)

4. **建 DB 骨架**:
   - `electron/main/db/database-manager.ts`
   - `electron/main/db/schema/index.ts` + `schema/logs.ts`
   - `electron/main/db/services/log.service.ts`

5. **產第一份 migration**:`npm run db:generate` → 確認 `0000_initial.sql` 內容對

6. **改 `electron.vite.config.ts`** 加 viteStaticCopy

7. **改主進程 `index.ts`**:try/catch init DatabaseManager;before-quit 內 close

8. **改主進程 `utils/logger.ts`**:加 `attachLogService` + 每個 level 多呼叫一行 `_logService?.write(...)`;writeRendererLog 同樣處理

9. **改 `ipc-handlers/log.handlers.ts`**:在原 writeRendererLog 之後加 `logService.write({source:'renderer', ...})`

10. **改渲染端 `src/utils/logger.ts`**:把 `forwardToMain` 從只 ERROR 改為所有等級

11. **驗證 dev**:任意觸發各等級日誌,VSCode SQLite Viewer 開 `<專案根>/app.db` 確認資料

12. **驗證 prod 打包**:`npm run build:win` → 安裝 → 重啟 → 查 `%APPDATA%\ichiaDesktop\app.db` 是否寫入正常 + txt 檔依然存在

---

## 20. 影響檔案清單

### 新增

| 檔案 | 用途 |
|---|---|
| `drizzle.config.ts` | drizzle-kit 設定 |
| `electron/main/db/database-manager.ts` | DB 單例 |
| `electron/main/db/schema/index.ts` | schema 出口 |
| `electron/main/db/schema/logs.ts` | logs 表 |
| `electron/main/db/services/log.service.ts` | LogService |
| `electron/main/db/migrations/0000_initial.sql` | drizzle-kit 產 |
| `electron/main/db/migrations/meta/*` | drizzle-kit 產 |

### 修改

| 檔案 | 動作 |
|---|---|
| [package.json](../package.json) | dep + devDep + db scripts + `build.asarUnpack` |
| [electron.vite.config.ts](../electron.vite.config.ts) | viteStaticCopy 拷貝 migrations |
| [electron/main/index.ts](../electron/main/index.ts) | DB init + attach + cleanup;close on quit |
| [electron/main/utils/logger.ts](../electron/main/utils/logger.ts) | 加 `attachLogService` + 各 level 多寫 DB;writeRendererLog 同 |
| [electron/main/ipc-handlers/log.handlers.ts](../electron/main/ipc-handlers/log.handlers.ts) | LOG_WRITE 在原寫 txt 之外多寫 DB |
| [electron/main/ipc-handlers/index.ts](../electron/main/ipc-handlers/index.ts) | `registerAllHandlers` 增 logService 參數 |
| [src/utils/logger.ts](../src/utils/logger.ts) | `forwardToMain` 從只 ERROR 改為所有等級(僅一行) |

### 不動

- [electron/main/utils/log-file-writer.ts](../electron/main/utils/log-file-writer.ts) — 完全保留,寫 txt 行為一字不改
- 既有的 [src/views/Settings/sections/LogSection.vue](../src/views/Settings/sections/LogSection.vue) — 「打開日誌資料夾」仍指 txt 資料夾,本期不變
- `electron/shared/ipc-channels.ts` 已有的 `LOG_WRITE` / `LOG_OPEN_FOLDER` 不動
- 渲染端業務代碼零修改
- ConfigManager / WindowManager / 自動更新 / 浮球 / AD 登入等模組全部不動

---

